<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Base application theme. Used by every Activity that does not opt
       into a more specific theme. Theme.AppCompat.Light.NoActionBar matches
       the rest of the Expo prebuild template. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:textColor">@android:color/black</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
  </style>

  <!-- Android 12+ splash screen via androidx.core:core-splashscreen.
       The system shows a small circular icon centred on the splash
       background colour; on Android 11 and below the library renders the
       same look using a window background fallback. expo-splash-screen 31
       cooperates with this theme via its own activity lifecycle hooks, so
       preventAutoHideAsync()/hideAsync() in JS still control when the
       splash is dismissed. -->
  <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
    <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>
</resources>
