# Local Flutter reference — not part of the RN app bundle or EAS upload.
src/flutter-code/

# Native build outputs — EAS runs its own prebuild.
android/
ios/

# Release artifacts — must never ship inside the app bundle.
releases/
*.aab
*.apk
